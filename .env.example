# Vercel Marketplace에서 Neon을 연결하면 자동으로 설정됩니다.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# /results에서 전체 응답을 내려받을 때 사용하는 관리자 비밀번호입니다.
EXPORT_SECRET=replace-with-a-long-random-password
